{
  "name": "pinflow-test-fixtures",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "packages/pinflow-test-fixtures/shared",
  "tags": ["scope:test", "type:test", "npm:private"],
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "eslintConfig": "eslint.config.mjs",
        "lintFilePatterns": [
          "packages/pinflow-test-fixtures/**/*.ts",
          "packages/pinflow-test-fixtures/**/*.tsx"
        ]
      }
    },
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{projectRoot}/test-output"],
      "options": {
        "config": "packages/pinflow-test-fixtures/vite.config.ts"
      },
      "configurations": {
        "watch": {
          "watch": true
        }
      }
    },
    "integration": {
      "executor": "@nx/vitest:test",
      "outputs": ["{projectRoot}/test-output"],
      "options": {
        "config": "packages/pinflow-test-fixtures/vite.config.ts",
        "testPathPattern": ["integration"]
      }
    },
    "e2e": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm exec playwright test",
        "cwd": "{projectRoot}"
      }
    },
    "preview-e2e": {
      "executor": "nx:run-commands",
      "options": {
        "command": "corepack pnpm exec playwright test preview-overlay-boot.spec.ts",
        "cwd": "{projectRoot}"
      }
    },
    "demo-e2e": {
      "executor": "nx:run-commands",
      "options": {
        "command": "corepack pnpm exec playwright test sixty-second-demo.spec.ts",
        "cwd": "{projectRoot}"
      }
    },
    "build": {
      "executor": "nx:noop"
    },
    "typecheck": {
      "executor": "nx:noop"
    },
    "watch-deps": {
      "executor": "nx:noop"
    },
    "build-deps": {
      "executor": "nx:noop"
    }
  }
}
