{
  "name": "pinflow",
  "version": "0.5.2",
  "description": "PinFlow is a pixel-to-code development tool, based on Domscribe, that maps running UI elements to their exact source locations and captures runtime context (props, state, DOM) for handoff to coding agents via MCP.",
  "mcpServers": {
    "pinflow": {
      "command": "npx",
      "args": [
        "-y",
        "pinflow",
        "mcp"
      ],
      "cwd": "${workspacePath}"
    }
  }
}
