/**
 * @pinflow/transform - AST injection of stable element IDs + bundler plugins
 *
 * Public API is exposed via subpath exports:
 * - `@pinflow/transform/plugins/vite`
 * - `@pinflow/transform/plugins/webpack`
 * - `@pinflow/transform/webpack-loader` (string path for webpack config)
 * - `@pinflow/transform/plugins/turbopack`
 * - `@pinflow/transform/turbopack-loader` (string path for Next.js turbopack config)
 *
 * @module @pinflow/transform
 */
